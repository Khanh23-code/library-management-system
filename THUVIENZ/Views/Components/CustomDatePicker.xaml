<UserControl x:Class="THUVIENZ.Views.Components.CustomDatePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root"
             Height="40">
    <Grid>
        <!-- Khung viền chọn ngày phong cách phẳng cao cấp -->
        <Border x:Name="borderWrapper" Background="#F3F4F6" BorderThickness="1" BorderBrush="#D1D5DB" CornerRadius="10" Cursor="Hand" MouseLeftButtonUp="BorderWrapper_MouseLeftButtonUp">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Chuỗi text ngày tháng hiển thị sắc nét -->
                <TextBlock x:Name="txtDateDisplay" Grid.Column="0" Text="--/--/----" FontSize="14" FontWeight="SemiBold" Foreground="#111827" VerticalAlignment="Center"/>

                <!-- Biểu tượng lịch sang trọng -->
                <TextBlock Grid.Column="1" Text="📅" FontSize="16" Foreground="#6B7280" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Popup chứa bảng chọn lịch hệ thống chuẩn xác -->
        <Popup x:Name="popupCalendar" StaysOpen="False" Placement="Bottom" AllowsTransparency="True" PopupAnimation="Fade">
            <Border Background="White" BorderThickness="1" BorderBrush="#E5E7EB" CornerRadius="12" Margin="0,4,0,0" Padding="4">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.15" Color="Black"/>
                </Border.Effect>
                
                <Calendar x:Name="calendarControl" BorderThickness="0" Background="Transparent" 
                          SelectedDatesChanged="CalendarControl_SelectedDatesChanged"/>
            </Border>
        </Popup>
    </Grid>
</UserControl>
